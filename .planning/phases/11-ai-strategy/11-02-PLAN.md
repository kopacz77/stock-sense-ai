---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/strategy-config.json
  - package.json
  - src/index.ts
  - src/analysis/technical-indicators.ts
  - src/strategy/types.ts
  - src/strategy/config.ts
  - src/strategy/substrate.ts
  - src/strategy/vix-provider.ts
  - src/strategy/levels.ts
  - src/strategy/sizing.ts
  - src/strategy/signals/sector-rotation.ts
  - src/strategy/strategy-engine.ts
  - src/strategy/decision-log.ts
  - src/strategy/cli/strategy-commands.ts
  - src/strategy/__tests__/tracer-e2e.test.ts
  - src/strategy/__tests__/levels.test.ts
  - src/strategy/__tests__/sizing.test.ts
  - src/strategy/__tests__/decision-log.test.ts
  - src/strategy/signals/__tests__/sector-rotation.test.ts
autonomous: true
requirements: [INCOME-01]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01/D-02: SECTOR_ROTATION_FROM_PM (a v1 core type) produces tagged, per-type-scored candidates end-to-end on the real 2026 substrate — one signal type proves the whole path before the other three are built"
    - "D-03: the SECTOR_ROTATION_FROM_PM score is exactly min(1, totalMovePp/10) * min(1, log10(totalVolume)/7) over pmContribution.sources, computed as a standalone pure function"
    - "D-05: the engine emits at most 5 ranked candidates above a 0.4 score floor and reports the next 3 sub-threshold candidates with their scores"
    - "D-06: every ranked candidate carries a dollar size derived from the live VIX regime (calm 25% / elevated 12.5% / stressed 6.25% of equity), with a max of 4 simultaneous positions and an assumedEquity fallback of 7500 until M2-02 ships"
    - "D-07: FADE_OVERSHOOT's 50% size modifier exists in the sizing table and is unit-tested, even though v1 never sizes a shadow candidate"
    - "D-08: every candidate carries a per-type entry, target, stop and timeHorizonDays derived from the CONTEXT ATR tables, with a uniform 1.5*ATR_5 stop"
    - "D-09: accepting a candidate logs the operator's chosen entry/target/stop/size, not the engine's suggestion, whenever an override is supplied"
    - "D-10: the engine emits no exit signal of any kind — once a position is open the operator manages it"
    - "D-11: every accept and skip appends one record to data/strategy/decisions-YYYY-MM-DD.jsonl in the CONTEXT-locked schema; records are never rewritten"
    - "D-12: an operator can log a realized close against a candidateId with an exit price and date"
    - "D-13: a 30-day accept/skip rate is computable and reported with the 20-40% sweet-spot band"
    - "D-14: on a day with nothing above the floor the engine emits an empty ranked list plus sub-threshold diagnostics — never a forced top-3"
    - "D-19: candidate ids follow ${YYYY-MM-DD}-${signalType}-${ticker}-${shortHash} and are collision-free for two same-day same-type same-ticker candidates"
    - "D-20: strategy code lives under src/strategy/ and every persisted strategy artifact lives under data/strategy/ — no strategy state is written into data/intel/, which stays M2-04's substrate"
    - "D-21: the engine runs as its own strategy run invocation, not inside the 60-90s scheduler runCycle"
  artifacts:
    - src/strategy/types.ts
    - src/strategy/config.ts
    - config/strategy-config.json
    - src/strategy/substrate.ts
    - src/strategy/vix-provider.ts
    - src/strategy/levels.ts
    - src/strategy/sizing.ts
    - src/strategy/signals/sector-rotation.ts
    - src/strategy/strategy-engine.ts
    - src/strategy/decision-log.ts
    - src/strategy/cli/strategy-commands.ts
  key_links:
    - "strategy-engine.ts reads TickerDaySummary rows through substrate.ts from ./data/intel — the M2-04 stream is the engine's only substrate input"
    - "TechnicalIndicators.calculateATRSeries is public and is the single ATR source for levels.ts"
    - "VixProvider calls the existing YahooFinanceProvider.fetchHistoricalDataRange('^VIX', from, to) — no new provider, no new credential"
    - "strategy run writes candidates to data/strategy/candidates-YYYY-MM-DD.jsonl so strategy accept/skip can resolve a candidateId from a later process"
    - "registerStrategyCommands(program) is called from src/index.ts beside registerIntelCommands(program)"
---

<objective>
Build the phase spine: one signal type (SECTOR_ROTATION_FROM_PM — the type with continuous, outage-proof PM substrate from 2026-05-23 onward) wired all the way from `data/intel/ticker-day-summary-*.jsonl` through scoring, ranking, VIX-regime sizing, ATR entry/target/stop, the decision log, and a working `strategy` CLI — then generalize the levels/sizing/decision-log layers so the remaining three signal types plug in without touching the architecture.

Purpose: `src/strategy/` is greenfield. Proving one vertical path end-to-end on real on-disk data — before building four signal modules against an unproven engine contract — is what catches an architectural dead end after one commit instead of after ten.

Output: the `src/strategy/` module skeleton, `config/strategy-config.json`, a public `TechnicalIndicators.calculateATRSeries`, and a `strategy` CLI group registered in `src/index.ts` that can generate, list, accept, skip and close candidates against today's real substrate.

This is the phase-level tracer. Plans 11-03 and 11-04 expand horizontally from it; nothing downstream may change the `SignalTypeModule` contract this plan establishes without saying so explicitly.
</objective>

<decision_map>
| ID | CONTEXT.md source |
|----|-------------------|
| D-01 | `### Signal Generation` — separate signal types, ranked together across types, type tag + per-type score |
| D-02 | `### v1 signal set (2026-08-27)` — SECTOR_ROTATION_FROM_PM is a core, ranked + sized v1 type |
| D-03 | `### Ranking` — pure per-type score formulas on native 0-1 scales |
| D-04 | `### Ranking` — no operator-tunable type weights in v1 |
| D-05 | `### Ranking` — top 5 max, 0.4 floor, next-3 sub-threshold shown |
| D-06 | `### Sizing` — VIX-regime % of equity, max 4 positions, $ amount, `assumedEquity` fallback |
| D-07 | `### Sizing` — FADE_OVERSHOOT sized at 50% of regime size |
| D-08 | `### Position Management` — per-type entry/target/stop/timeHorizonDays |
| D-09 | `### Position Management` — operator override logged in place of the suggestion |
| D-10 | `### Position Management` — no exit signals in v1 |
| D-11 | `### Decision Tracking` — decision log path + record shape, append-only |
| D-12 | `### Decision Tracking` — operator-driven outcome close |
| D-13 | `### Decision Tracking` — 30-day accept/skip rate, 20-40% sweet spot |
| D-14 | `### Empty State` — honest no-candidates output, no forced top-3 |
| D-19 | `### Claude's Discretion` — candidate id format |
| D-20 | `### Claude's Discretion` — paths under `data/strategy/` and `src/strategy/` |
| D-21 | `### Claude's Discretion` — separate strategy tick vs. wiring into `runCycle` (RESEARCH Open Question 3 recommends separate) |
</decision_map>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-strategy/11-CONTEXT.md
@.planning/phases/11-ai-strategy/11-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
**New module `src/strategy/`**
- `types.ts` — `SignalType`, `VixRegime`, `SignalMode` (`"core" | "gated" | "shadow"`), `CandidateMode` (`"ranked" | "sub-threshold" | "shadow"`), `TargetSpec`, `RawSignal`, `SignalContext`, `SignalTypeModule`, `StrategyCandidate`, `StrategyDecisionRecord`, `StrategyRunResult`
- `config.ts` — `StrategyConfig` interface, `loadStrategyConfig(path?)`, `DEFAULT_STRATEGY_CONFIG`
- `substrate.ts` — `loadRollupsForDay`, `loadRollupsForRange`, `isSubstrateHot`
- `vix-provider.ts` — `VixQuote`, `VixProvider`, `classifyRegime`
- `levels.ts` — `computeLevels`, `LevelInputs`, `ComputedLevels`, `ATR_STOP_MULTIPLE`, `MAX_STOP_DISTANCE_PCT`, `MAX_ATR_TARGET_DISTANCE_PCT`
- `sizing.ts` — `suggestSizeUsd`, `regimeSizePct`, `grossExposureMultiple`, `TYPE_SIZE_MODIFIER`
- `signals/sector-rotation.ts` — `scoreSectorRotation`, `SectorRotationModule`
- `strategy-engine.ts` — `StrategyEngine`, `StrategyEngineOptions`, `buildCandidateId`
- `decision-log.ts` — `DecisionLog` (`recordAccept`, `recordSkip`, `recordClose`, `readDay`, `readRange`, `findCandidate`, `readDedupedByCandidateId`, `acceptSkipStats`)
- `cli/strategy-commands.ts` — `registerStrategyCommands(program)`

**New CLI subcommands** (this plan): `strategy run`, `strategy list-candidates`, `strategy accept`, `strategy skip`, `strategy close`, `strategy decisions-summary`, `strategy show-vix`

**New config / scripts**
- `config/strategy-config.json`
- `package.json` script `"strategy": "tsx src/index.ts strategy"`

**New JSONL streams under `data/strategy/`**
- `candidates-YYYY-MM-DD.jsonl` (engine output, one row per generated candidate incl. sub-threshold and shadow)
- `decisions-YYYY-MM-DD.jsonl` (accept / skip / close events, append-only)
- `vix-cache.json` (last-known VIX close + regime per date)

**Modified existing symbol**
- `TechnicalIndicators.calculateATRSeries(data: PriceData, period: number): number[]` — new public static on `src/analysis/technical-indicators.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "PM signal to sized trade idea" — SECTOR_ROTATION_FROM_PM only, one path</name>
  <files>config/strategy-config.json, src/strategy/config.ts, src/strategy/types.ts, src/strategy/substrate.ts, src/strategy/vix-provider.ts, src/analysis/technical-indicators.ts, src/strategy/signals/sector-rotation.ts, src/strategy/levels.ts, src/strategy/sizing.ts, src/strategy/strategy-engine.ts, src/strategy/decision-log.ts, src/strategy/cli/strategy-commands.ts, src/index.ts, package.json, src/strategy/__tests__/tracer-e2e.test.ts, src/strategy/signals/__tests__/sector-rotation.test.ts</files>
  <read_first>
    - `src/market-intelligence/signal/types.ts` lines 111-154 (`TickerDaySummary`, especially `pmContribution.netScore` and `pmContribution.sources[]` with `movePp`, `direction`, `weight`, `contributedScore`, `volume24hr`) — the exact substrate this task consumes
    - `src/market-intelligence/storage/jsonl-store.ts` (whole file, ~100 lines) — `append`, `appendMany`, `appendManyOn`, `readDay`, `readRecent`, `fileFor`; this is the only persistence primitive you may use
    - `src/market-intelligence/signal/pm-mapping-engine.ts` lines 16-35 (worked-example doc-comment style) and lines 59-108 (`TickerSignal` interface, `PmMappingEngineOptions` `{ dataDir }` constructor shape to mirror)
    - `src/analysis/technical-indicators.ts` lines 1-20 (`technicalindicators` imports, `PriceData` interface) and lines 248-259 (the existing `private static async calculateATR` you are adding a public sibling to)
    - `src/data/providers/yahoo-finance-provider.ts` lines 63-95 (`fetchHistoricalDataRange`, and the comment stating the parsed result is sorted **newest-first**)
    - `src/data/market-data-service.ts` lines 473-530 (`fetchHistoricalData(symbol, from, to): Promise<OHLCVData[]>`, provider fallback chain ending at Yahoo)
    - `src/backtesting/strategies/strategy-adapter.ts` lines 1-50 (the bar-ordering doc-comment discipline that M2-01's SELL-condition bug demands you copy)
    - `src/market-intelligence/cli/intel-commands.ts` lines 1-33 (imports) and lines 739-780 (`backlog-drain` subcommand: `.option()`, `Number.isFinite` validation, `process.exit(2)`, chalk status lines, empty-state early return)
    - `src/index.ts` lines 18-30 and lines 1025-1045 (`registerBacktestCommands(program)` line 1030, `registerIntelCommands(program)` line 1041 — the registration site)
    - `src/market-intelligence/signal/__tests__/rollup-backfill.test.ts` (the `fs.mkdtemp` isolated-dataDir + `fixture(overrides)` + `writeFixtureFile` test shape every `src/strategy` test must follow)
    - `.planning/phases/11-ai-strategy/11-PATTERNS.md` (all sections)
  </read_first>
  <action>
    Wire ONE entry point through every layer of this phase, for SECTOR_ROTATION_FROM_PM only. No other signal type, no batching, no backtest. Real error handling on this single path. This slice is production code that later tasks build on, not a prototype — the only things allowed to be missing are *other signal types*, never architectural pieces.

    File count exceeds the usual per-task guidance on purpose: `src/strategy/` is greenfield, so the thinnest end-to-end path still needs one file per layer. Keep each file minimal.

    **1. `config/strategy-config.json`** — follow `config/macro-tickers.json`'s `version` / `lastUpdated` header and `config/pm-market-mappings.json`'s `addedBy` provenance convention. Keys: `version: 1`, `lastUpdated`, `addedBy: "manual-2026-08-27"`, `assumedEquity: 7500`, `scoreFloor: 0.4`, `maxCandidatesPerDay: 5`, `subThresholdCount: 3`, `maxSimultaneousPositions: 4`, `vixThresholds: { "calmBelow": 15, "stressedAbove": 25 }`, `regimeSizePct: { "calm": 0.25, "elevated": 0.125, "stressed": 0.0625 }`, `typeSizeModifier: { "FADE_OVERSHOOT": 0.5 }`, `signalModes: { "CATALYST_ANCHORED": "core", "SECTOR_ROTATION_FROM_PM": "core", "SENTIMENT_VELOCITY": "gated", "FADE_OVERSHOOT": "shadow" }`, `intelDataDir: "./data/intel"`, `strategyDataDir: "./data/strategy"`.

    **2. `src/strategy/config.ts`** — `StrategyConfig` interface mirroring that JSON exactly, `DEFAULT_STRATEGY_CONFIG` with the same values as a literal, and `loadStrategyConfig(configPath = "./config/strategy-config.json"): Promise<StrategyConfig>` that reads the file, shallow-merges over the defaults, and throws a named error listing the offending key when `assumedEquity <= 0`, `scoreFloor` is outside [0,1], or `maxCandidatesPerDay < 1`.

    **3. `src/strategy/types.ts`** — the contract between `signals/*`, the engine, the decision log and the CLI. Copy the doc-comment and per-field-unit-comment discipline of `src/market-intelligence/signal/types.ts` lines 1-11 and 111-154, and import `TickerDaySummary`, `CatalystFlag`, `CatalystType`, `CatalystDirection` from `../market-intelligence/signal/types.js` rather than redefining them. Declare: `SignalType`, `VixRegime`, `SignalMode`, `CandidateMode`; `TargetSpec` as the discriminated union `{ kind: "atr"; period: 3 | 5 | 10; multiple: number } | { kind: "pctOfClose"; pct: number } | { kind: "absoluteMove"; move: number }`; `RawSignal` as `{ signalType; ticker; score; direction: "long" | "short"; rationale; entryStyle: "close" | "pullback"; targetSpec: TargetSpec; timeHorizonDays: number; sizeModifier?: number; sourceArticleIds: string[]; sourcePmMarkets: Array<{ marketId: string; slug: string; movePp: number }>; sourceCatalystId?: string }` — `sizeModifier` defaults to 1 and exists so a `binary`-direction catalyst can emit its two opposite-direction candidates at 0.5 each (RESEARCH §4 `dirSign` rules) without any later plan editing this file; `SignalContext` as `{ asOfDate: string; rollups: TickerDaySummary[]; config: StrategyConfig; intelDataDir: string }` (later plans extend it — add fields, never repurpose existing ones); `SignalTypeModule` as `{ readonly signalType: SignalType; readonly mode: SignalMode; generate(ctx: SignalContext): Promise<RawSignal[]>; gate?(ctx: SignalContext): Promise<{ ok: boolean; reason: string }> }` — the optional `gate` exists so a `mode: "gated"` type can decline to run with a stated reason instead of silently emitting nothing (D-02's "never a silent zero-delta"); the engine calls it before `generate` and records the reason in `StrategyRunResult.skippedTypes`; `StrategyCandidate` = `RawSignal` fields plus `candidateId`, `generatedAt`, `asOfDate`, `mode: CandidateMode`, `vixRegime`, `vixCloseAtGeneration`, `vixSource: "live" | "cache" | "fallback"`, `suggestedEntry`, `suggestedTarget`, `suggestedStop`, `suggestedSizeUsd: number | null`, `atrPeriodUsed: 3 | 5 | 10`, `atrValue`; `StrategyDecisionRecord` exactly as RESEARCH.md §5 specifies, plus `mode: CandidateMode` (D-02 requires shadow records to carry `mode: "shadow"`); `StrategyRunResult` as `{ asOfDate; vix: VixQuote; ranked: StrategyCandidate[]; subThreshold: StrategyCandidate[]; shadow: StrategyCandidate[]; skippedTypes: Array<{ signalType: SignalType; reason: string }> }`.

    **4. `src/strategy/substrate.ts`** — `loadRollupsForDay(intelDataDir: string, date: Date): Promise<TickerDaySummary[]>` via `new JsonlStore<TickerDaySummary>(intelDataDir, "ticker-day-summary").readDay(date)`. Also export `loadRollupsForRange(intelDataDir, startIso, endIso): Promise<Map<string, TickerDaySummary[]>>` keyed by `YYYY-MM-DD` (later plans need history; implement it now so the read path exists in one place). Before reading a day within 48 hours of now, call `isDrainLocked(intelDataDir)` from `../market-intelligence/signal/backlog-drain.js` and, when locked, return the rows anyway but attach nothing — instead surface a boolean from a second export `isSubstrateHot(intelDataDir, date): Promise<boolean>` so callers can warn (RESEARCH Pitfall 4: `intel backlog-drain` rewrites rollup files mid-run).

    **5. `src/analysis/technical-indicators.ts`** — add `static calculateATRSeries(data: PriceData, period: number): number[]` immediately above the existing private `calculateATR`. It calls `ATR.calculate({ high: data.high, low: data.low, close: data.close, period })` and returns the full series unchanged. Document in a doc comment that the returned array is shorter than the input by `period - 1`, that the LAST element is the most recent ATR, and that callers must pass **oldest-first** arrays. Do not change `calculateATR` or any existing caller.

    **6. `src/strategy/vix-provider.ts`** — `export function classifyRegime(vixClose: number, thresholds: { calmBelow: number; stressedAbove: number }): VixRegime` returning `"calm"` when `vixClose < calmBelow`, `"stressed"` when `vixClose > stressedAbove`, else `"elevated"` (so 15 and 25 both land in `elevated`). `export interface VixQuote { date: string; close: number; regime: VixRegime; source: "live" | "cache" | "fallback"; fetchedAt: string }`. `export class VixProvider` with the `{ dataDir }` options-object shape from `pm-mapping-engine.ts:73-80`: options `{ strategyDataDir?: string; thresholds?; provider?: YahooFinanceProvider }`, cache at `${strategyDataDir}/vix-cache.json` as `Record<string, VixQuote>` keyed by `YYYY-MM-DD`. `getForDate(date)` returns the cached quote when present; otherwise fetches `provider.fetchHistoricalDataRange("^VIX", date - 7 days, date)`, **sorts the returned bars ascending by `date` with an explicit one-line rationale comment naming the newest-first Yahoo ordering** (`strategy-adapter.ts` doc-comment discipline), takes the newest bar whose date is `<= asOfDate`, writes the cache atomically (temp file + `fs.rename`, same as `RollupBuilder.writeSummary` at `rollup-builder.ts:193-203`), and returns `source: "live"`. On any fetch failure return `{ close: lastKnownCacheClose ?? 20, regime: "elevated", source: "fallback" }` — never `"calm"` (RESEARCH Pitfall 2), and log one `console.warn` naming the failure.

    **7. `src/strategy/signals/sector-rotation.ts`** — `export function scoreSectorRotation(rollup: TickerDaySummary): number` implementing D-03's formula verbatim: `ppNorm = Math.min(1, sum(|movePp * weight|) / 10)`, `volNorm = totalVolume > 0 ? Math.min(1, Math.log10(totalVolume) / 7) : 0`, return `ppNorm * volNorm`. Direction is `sign(pmContribution.netScore)` mapped to `"long"` / `"short"`; a `netScore` of exactly 0 or an empty `sources[]` emits no signal. `export class SectorRotationModule implements SignalTypeModule` with `signalType = "SECTOR_ROTATION_FROM_PM"`, `mode = "core"`, and `generate(ctx)` mapping qualifying rollups to `RawSignal`s with `entryStyle: "close"`, `targetSpec: { kind: "atr", period: 10, multiple: 2.0 }`, `timeHorizonDays: 12` (midpoint of CONTEXT's 10-15), `sourcePmMarkets` projected from `pmContribution.sources` as `{ marketId, slug, movePp }`, `sourceArticleIds: []`, and a `rationale` naming the top contributing market slug, its `movePp`, and the resulting bias — e.g. the Iran-ceasefire → XLE shape CONTEXT calls the canonical happy path. Carry a worked-example doc comment in the `pm-mapping-engine.ts:16-35` style.

    **8. `src/strategy/levels.ts` and `src/strategy/sizing.ts`** — minimal versions sufficient for this one type; Task 2 generalizes them. `levels.ts`: `computeLevels({ close, direction, atrByPeriod, entryStyle, targetSpec })` returning `{ entryPrice, targetPrice, stopPrice, atrPeriodUsed, atrValue }` with `dirSign = direction === "long" ? 1 : -1`, entry `close` for `entryStyle: "close"`, stop `entry - 1.5 * ATR_5 * dirSign`, ATR target `close + multiple * ATR_period * dirSign`. `sizing.ts`: `suggestSizeUsd(regime, signalType, equity, config)` = `Math.floor(equity * config.regimeSizePct[regime] * (config.typeSizeModifier[signalType] ?? 1))`.

    **9. `src/strategy/strategy-engine.ts`** — `export class StrategyEngine` with options `{ intelDataDir?; strategyDataDir?; configPath?; modules?: SignalTypeModule[]; vixProvider?; marketData? }` (defaults: config-file dirs, `[new SectorRotationModule()]`, a fresh `VixProvider`, a fresh `MarketDataService`). `generateCandidates(asOfDate: Date): Promise<StrategyRunResult>`: load config, load the day's rollups, run each module's `generate`, fetch the VIX quote once, fetch ATR inputs per surviving ticker via `MarketDataService.fetchHistoricalData(ticker, asOfDate - 120 days, asOfDate)` (sort ascending by date before building `PriceData`, with the rationale comment), compute levels and size, build candidates, then partition: candidates with `score >= config.scoreFloor` sorted descending become `ranked` capped at `config.maxCandidatesPerDay`; the next `config.subThresholdCount` by score become `subThreshold` with `mode: "sub-threshold"` and `suggestedSizeUsd: null`; `shadow` stays empty in this task. Export `buildCandidateId(asOfDate, signalType, ticker, score, generatedAt)` = `` `${asOfDate}-${signalType}-${ticker}-${shortHash}` `` where `shortHash` is the first 8 hex chars of a `node:crypto` sha256 over `` `${signalType}|${ticker}|${asOfDate}|${score.toFixed(4)}|${generatedAt}` `` — the `generatedAt` component is what keeps two same-day same-type same-ticker candidates distinct (RESEARCH § Known Threat Patterns, `candidateId` collision). Emit no exit signal, no "take profit" and no "stop out" instruction anywhere (D-10). Persist every candidate — ranked, sub-threshold and shadow alike — to `data/strategy/candidates-YYYY-MM-DD.jsonl` via `JsonlStore.appendManyOn(candidates, asOfDate)` so a later CLI process can resolve a `candidateId`. Do not wire any of this into `src/market-intelligence/scheduler/cycle-runner.ts` (D-21).

    **10. `src/strategy/decision-log.ts`** — `export class DecisionLog` wrapping `new JsonlStore<StrategyDecisionRecord>(strategyDataDir, "decisions")`, with `recordAccept(candidate, overrides, note?)` and `recordSkip(candidate, note?)` for this task. Accept writes `decision: "accept"`, `decidedAt`, and `operatorEntry/operatorTarget/operatorStop/operatorSizeUsd` set to the override when supplied and to the engine suggestion when not; skip writes `decision: "skip"` with all four operator fields `null`. Append-only — never rewrite a past record.

    **11. `src/strategy/cli/strategy-commands.ts`** — `export function registerStrategyCommands(program: Command): void` creating a `strategy` command with hyphenated single-word subcommands only (Commander treats `.command("list candidates")` as one literal name — see RESEARCH §6 and the `intel themes-review` / `intel backlog-drain` precedents). This task ships three: `strategy run [--date YYYY-MM-DD] [--max-candidates <n>] [--dry-run]`, `strategy accept <candidateId> [--entry <n>] [--target <n>] [--stop <n>] [--size <n>] [--note <text>]`, `strategy skip <candidateId> [--note <text>]`. Validate every numeric option with `Number.isFinite` and `> 0`, printing an error and `process.exit(2)` on failure — copy the `backlog-drain` block's validation shape exactly. `strategy run` prints the VIX close + regime header, then the ranked candidates with type, ticker, score, entry/target/stop, `$` size and rationale, then a clearly-labelled sub-threshold section. When `ranked` is empty print an explicit no-candidates-above-threshold line and still print the sub-threshold section (D-14) — do not backfill the list to reach three or five. `--dry-run` prints without writing `candidates-*.jsonl`.

    **12. `src/index.ts`** — add `import { registerStrategyCommands } from "./strategy/cli/strategy-commands.js";` beside the existing `registerIntelCommands` import (line 24) and call `registerStrategyCommands(program);` beside line 1041. **13. `package.json`** — add `"strategy": "tsx src/index.ts strategy"` next to the existing `"intel"` script.

    **14. `src/strategy/__tests__/tracer-e2e.test.ts`** — the end-to-end check for this slice, not a per-layer unit test. Using the `fs.mkdtemp` isolated-dir pattern from `rollup-backfill.test.ts`: write a fixture `ticker-day-summary-2026-06-25.jsonl` containing an XLE row whose `pmContribution.sources` carries an Iran-ceasefire-shaped market (`slug` containing `iran`, `movePp: 12`, `direction: "long"`, `weight: 0.8`, `volume24hr: 2_000_000`) plus a second, lower-scoring ticker; inject a stub VIX provider returning `{ close: 18, regime: "elevated", source: "live" }` and a stub market-data source returning a deterministic 120-bar OHLCV series; run `StrategyEngine.generateCandidates(new Date("2026-06-25"))`; assert one ranked XLE long candidate with a score matching the hand-computed D-03 formula value, a `suggestedSizeUsd` of `Math.floor(7500 * 0.125)` = 937, non-null entry/target/stop with `stop < entry < target`, a `candidateId` matching `/^2026-06-25-SECTOR_ROTATION_FROM_PM-XLE-[0-9a-f]{8}$/`, and a written `candidates-2026-06-25.jsonl`; then call `DecisionLog.recordAccept` with an operator entry override and assert the persisted `decisions-*.jsonl` row carries the operator's number rather than the engine's.
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy/__tests__/tracer-e2e.test.ts && pnpm tsc --noEmit && pnpm dev strategy run --date 2026-08-26 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/strategy/__tests__/tracer-e2e.test.ts` exits 0
    - `pnpm tsc --noEmit` exits 0 and `pnpm biome check src/` exits 0
    - `pnpm dev strategy run --date 2026-08-26 --dry-run` exits 0 against the real `data/intel` tree and prints a VIX regime header plus either ranked candidates or the explicit no-candidates line, followed by a sub-threshold section
    - `grep -c "calculateATRSeries" src/analysis/technical-indicators.ts` returns at least 1 and `pnpm vitest run` shows no regression in the pre-existing suite
    - `grep -c "registerStrategyCommands" src/index.ts` returns 2 (import + call)
    - `config/strategy-config.json` parses as JSON and contains `assumedEquity`, `scoreFloor`, `maxCandidatesPerDay`, `signalModes`
    - `node -e "const c=require('./config/strategy-config.json');process.exit(c.scoreFloor===0.4&&c.maxCandidatesPerDay===5&&c.assumedEquity===7500?0:1)"` exits 0
  </acceptance_criteria>
  <done>A PM-derived sector-rotation signal on real 2026 data becomes a ranked, sized, level-bearing trade idea in the CLI and an accepted decision in `data/strategy/decisions-*.jsonl`, end to end, and is committed.</done>
  <reversibility rating="costly">`data/strategy/candidates-*.jsonl` and `decisions-*.jsonl` become persisted formats other tooling will read. Schema changes after operator data accumulates require a migration, so the record shapes are locked to the CONTEXT §Decision Tracking schema and extended only additively.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize levels and sizing to all four signal types</name>
  <files>src/strategy/levels.ts, src/strategy/sizing.ts, src/strategy/strategy-engine.ts, src/strategy/__tests__/levels.test.ts, src/strategy/__tests__/sizing.test.ts</files>
  <read_first>
    - `src/strategy/levels.ts` and `src/strategy/sizing.ts` (Task 1 output)
    - `src/strategy/types.ts` (Task 1 output — `TargetSpec`, `SignalType`)
    - `.planning/phases/11-ai-strategy/11-CONTEXT.md` `### Position Management` and `### Sizing`
    - `.planning/phases/11-ai-strategy/11-RESEARCH.md` §4 (the entry/target/stop table, the CATALYST per-type target table, the `dirSign` rules, the `suggestSizeUsd` snippet) and §9 Pitfall 5
    - `src/analysis/technical-indicators.ts` (the `calculateATRSeries` added in Task 1)
  </read_first>
  <behavior>
    - `computeLevels` with `entryStyle: "pullback"` and `direction: "long"` returns `entryPrice === Math.max(close - 0.5 * atr5, close * 0.99)`; with `direction: "short"` it returns `Math.min(close + 0.5 * atr5, close * 1.01)`.
    - `computeLevels` with `entryStyle: "close"` returns `entryPrice === close` for both directions.
    - The stop is always `entry - 1.5 * atr5 * dirSign`, clamped so it is never further than 5% from entry (long: `Math.max(entry - 1.5*atr5, entry*0.95)`; short: `Math.min(entry + 1.5*atr5, entry*1.05)`) — Pitfall 5.
    - An `{ kind: "atr" }` target is clamped so it is never further than 15% from entry; a `{ kind: "pctOfClose" }` target (the FDA 25% case) and an `{ kind: "absoluteMove" }` target (the earnings average-historical-move case) are NOT clamped, because those distances are deliberate rather than an ATR artifact.
    - `targetPriceForCatalyst(catalystType, close, dirSign, atrByPeriod, avgHistoricalEarningsMove?)` returns the RESEARCH §4 table: `fda`/`fda_pdufa` -> 25% of close; `earnings` -> the supplied average historical move, falling back to `2 * ATR_5` when it is absent; `treasury_auction` -> `1 * ATR_5`; every other `CatalystType` -> `2 * ATR_5`.
    - `suggestSizeUsd("calm", "SECTOR_ROTATION_FROM_PM", 7500, config)` is 1875; `"elevated"` is 937; `"stressed"` is 468.
    - `suggestSizeUsd(regime, "FADE_OVERSHOOT", ...)` is exactly half the same-regime size for any other type (D-07), even though v1 never sizes a shadow candidate.
    - `grossExposureMultiple(regime, config)` returns 1.0 / 0.5 / 0.25 for calm / elevated / stressed, i.e. `maxSimultaneousPositions * regimeSizePct`.
    - A `direction` of `"uncertain"` is rejected at the type level (`RawSignal.direction` is only `"long" | "short"`); the caller mapping a `CatalystDirection` is responsible for skipping `"uncertain"` and splitting `"binary"` — assert this with a type-level test comment plus a runtime guard that throws on an unexpected direction string.
  </behavior>
  <action>
    Replace the Task-1 minimal `levels.ts` / `sizing.ts` with the full CONTEXT tables, so 11-03 and 11-04 can plug their signal types in without touching either file again.

    `levels.ts` exports: `ATR_STOP_MULTIPLE = 1.5`, `MAX_STOP_DISTANCE_PCT = 0.05`, `MAX_ATR_TARGET_DISTANCE_PCT = 0.15`, `PULLBACK_ATR_MULTIPLE = 0.5`, `PULLBACK_FLOOR_PCT = 0.01`, plus `computeLevels(inputs: LevelInputs): ComputedLevels` and `targetPriceForCatalyst(...)`. `LevelInputs` is `{ close: number; direction: "long" | "short"; atrByPeriod: Record<3 | 5 | 10, number>; entryStyle: "close" | "pullback"; targetSpec: TargetSpec }`. `ComputedLevels` is `{ entryPrice; targetPrice; stopPrice; atrPeriodUsed: 3 | 5 | 10; atrValue: number }`, where `atrPeriodUsed`/`atrValue` report the period the *target* used (the stop always uses ATR_5; say so in the doc comment so a reader is not misled).

    Round all emitted prices to 2 decimals with a single shared helper so the CLI and the decision log never disagree by a floating-point epsilon.

    `sizing.ts` exports `TYPE_SIZE_MODIFIER: Record<SignalType, number>` (`FADE_OVERSHOOT: 0.5`, all others `1`), `regimeSizePct(regime, config)`, `suggestSizeUsd(regime, signalType, equity, config, signalSizeModifier = 1)` = `Math.floor(equity * regimeSizePct * typeModifier * signalSizeModifier)` (the trailing argument is `RawSignal.sizeModifier`, which lets a `binary` catalyst halve each of its two legs without a second sizing rule), and `grossExposureMultiple(regime, config)`. Keep the equity value coming from `config.assumedEquity` with a doc comment stating this is the M2-02 fallback and naming the broker-balance field that will replace it.

    Update `strategy-engine.ts` to call the generalized functions; the SECTOR_ROTATION candidate values from Task 1's tracer test must not change.

    Write `src/strategy/__tests__/levels.test.ts` and `src/strategy/__tests__/sizing.test.ts` covering every `<behavior>` bullet, including a low-priced-stock case (close 3.20, ATR_5 0.85) that exercises both Pitfall-5 clamps and a high-priced case that does not.
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy/__tests__/levels.test.ts src/strategy/__tests__/sizing.test.ts src/strategy/__tests__/tracer-e2e.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy/levels.ts` contains `export function computeLevels(`, `export function targetPriceForCatalyst(`, `ATR_STOP_MULTIPLE`, `MAX_STOP_DISTANCE_PCT`, `MAX_ATR_TARGET_DISTANCE_PCT`
    - `src/strategy/sizing.ts` contains `export function suggestSizeUsd(`, `export function grossExposureMultiple(`, `TYPE_SIZE_MODIFIER`
    - `pnpm vitest run src/strategy` exits 0 with the three test files green and the tracer test still passing unchanged
    - A sizing test asserts `suggestSizeUsd("elevated", "FADE_OVERSHOOT", 7500, cfg) === Math.floor(suggestSizeUsd("elevated", "CATALYST_ANCHORED", 7500, cfg) / 2)`
    - A levels test asserts the low-priced case clamps both stop and ATR target, and that a `pctOfClose` target is left unclamped
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All four signal types' entry/target/stop and sizing rules exist as tested pure functions; the tracer's numbers are unchanged.</done>
  <reversibility rating="reversible">Multipliers and clamps are named constants in one file; retuning them is a constant change with no persisted-data impact (CONTEXT explicitly leaves ATR multipliers to Claude's discretion).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Decision-log reconcile semantics and the operator's full accept/skip/close/report loop</name>
  <files>src/strategy/decision-log.ts, src/strategy/cli/strategy-commands.ts, src/strategy/__tests__/decision-log.test.ts</files>
  <read_first>
    - `src/strategy/decision-log.ts` (Task 1 output)
    - `src/strategy/cli/strategy-commands.ts` (Task 1 output)
    - `.planning/phases/11-ai-strategy/11-RESEARCH.md` §5 (the full `StrategyDecisionRecord` schema, the append-plus-reconcile-at-read rule) and §6 (the CLI surface list)
    - `src/market-intelligence/signal/backlog-drain.ts` lines 74-98 (`persistDrainedRecords` — the `byDay` Map + `appendManyOn` day-bucketing precedent) and lines 34-131 (the lock-file constants and helpers)
    - `src/market-intelligence/cli/intel-commands.ts` lines 739-780 (option validation and chalk output shape)
    - `src/market-intelligence/signal/__tests__/backlog-drain.test.ts` (test structure for a store-backed module)
  </read_first>
  <behavior>
    - `readDedupedByCandidateId(start, end)` returns one record per `candidateId`, keeping the record with the latest `closedAt ?? decidedAt`.
    - A skip followed by an accept for the same `candidateId` reconciles to the accept; both raw rows remain on disk.
    - `recordClose(candidateId, { exitPrice, exitDate, note })` appends a new record carrying the same `candidateId`, the prior record's fields, `closedAt`, `closeExitPrice`, `closeRealizedPnlUsd` and `closeRealizedPnlPct`, computed from the *operator's* entry and size, not the engine suggestion (D-09/D-12).
    - Closing a `candidateId` that was skipped, or that does not exist, fails with a named error rather than writing a record.
    - `acceptSkipStats(days)` over a fixture window returns `{ accepted, skipped, total, acceptRate, band }` where `band` is `"low"` below 0.10, `"high"` above 0.60, and `"sweet-spot"` between 0.20 and 0.40 inclusive (D-13).
    - A short-direction close computes profit when the exit price is below the operator entry.
  </behavior>
  <action>
    Complete the decision log and the operator loop.

    `decision-log.ts` gains `recordClose`, `readDay`, `readRange(startIso, endIso)`, `findCandidate(candidateId)` (scans `candidates-*.jsonl` across the range, newest first), `readDedupedByCandidateId`, and `acceptSkipStats(days)`. All writes go through `JsonlStore.appendManyOn(records, day)` day-bucketed exactly like `persistDrainedRecords`, so a close logged today against a candidate generated last week lands in today's file while still reconciling by `candidateId` at read time. Never rewrite or delete an existing row.

    P&L on close: `closeRealizedPnlUsd = (exitPrice - operatorEntry) * dirSign * (operatorSizeUsd / operatorEntry)`, `closeRealizedPnlPct = (exitPrice - operatorEntry) / operatorEntry * dirSign * 100`, both rounded to 2 decimals, with `dirSign` from the record's `direction`.

    Add the remaining CLI subcommands to `strategy-commands.ts`, all hyphenated single words: `strategy list-candidates [--date YYYY-MM-DD] [--include-skipped] [--include-closed]`, `strategy close <candidateId> --exit-price <n> [--exit-date YYYY-MM-DD] [--note <text>]`, `strategy decisions-summary [--days 30]`, `strategy show-vix [--date YYYY-MM-DD]`. `decisions-summary` prints accepted/skipped/total, the accept rate to one decimal, the band verdict, and the literal reminder that 20-40% is the intended range. `show-vix` prints the close, the regime and whether the value came from a live fetch, the cache or the conservative fallback.

    Input validation (ASVS V5, RESEARCH § Known Threat Patterns): every numeric option goes through `Number.isFinite` and a positivity check with `process.exit(2)` on failure; an unknown `candidateId` prints a clear not-found error and exits 1 rather than silently no-op'ing; `--size` larger than twice the largest regime size for the configured `assumedEquity` prints a warning naming both numbers and still proceeds (the operator is trusted; this catches a fat-finger, per RESEARCH's stated philosophy). Treat `--note` as an opaque display string — never interpolate it into a shell command or a regular expression.

    Write `src/strategy/__tests__/decision-log.test.ts` covering every `<behavior>` bullet with the `fs.mkdtemp` isolated-dir pattern.
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy/decision-log.ts` contains `recordAccept`, `recordSkip`, `recordClose`, `readDedupedByCandidateId`, `acceptSkipStats`
    - `pnpm vitest run src/strategy/__tests__/decision-log.test.ts` exits 0 with a case proving skip-then-accept reconciles to accept while both rows persist on disk
    - `strategy-commands.ts` registers `run`, `list-candidates`, `accept`, `skip`, `close`, `decisions-summary`, `show-vix` — verify with `pnpm dev strategy --help` listing all seven
    - `pnpm dev strategy close does-not-exist --exit-price 10` exits with a non-zero code and prints a not-found message
    - `pnpm dev strategy accept some-id --entry abc` exits with code 2
    - `pnpm vitest run` exits 0 (full suite) and `pnpm biome check src/` exits 0
  </acceptance_criteria>
  <done>The operator can generate, list, accept with overrides, skip, close with a realized P&L, and read a 30-day accept-rate report — all against append-only files that reconcile correctly at read time.</done>
  <reversibility rating="costly">The reconcile-by-`candidateId` read rule is a persisted-format contract; every future reader of `decisions-*.jsonl` must apply it. Changing it later requires re-processing accumulated operator history.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI args → `data/strategy/*.jsonl` | `--entry`, `--target`, `--stop`, `--size`, `--exit-price`, `--date`, `--note`, `<candidateId>` cross into file writes and (in later milestones) into real order sizing |
| Yahoo Finance response → engine | Untrusted external JSON becomes the VIX close that drives every position size |
| `data/intel/*.jsonl` (LLM-authored content) → rationale strings | Model-generated text flows into candidate rationale printed to a terminal and later to a browser |
| Concurrent writer (`intel backlog-drain`) → substrate read | A second process rewrites `ticker-day-summary-*.jsonl` while the engine reads it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Tampering | CLI numeric overrides (`--entry`/`--target`/`--stop`/`--size`/`--exit-price`) | medium | mitigate | `Number.isFinite` + positivity check with `process.exit(2)`; `--size` above 2x the largest regime size for `assumedEquity` emits a warning naming both figures before it is persisted (Task 3) |
| T-11-02-02 | Tampering | Malformed / adversarial `--note` text persisted then rendered | low | mitigate | `operatorNote` and `closeOperatorNote` are opaque display strings — never interpolated into a shell command, a `RegExp`, or a template evaluated at runtime |
| T-11-02-03 | Spoofing | `candidateId` collision across two same-day same-type same-ticker candidates | medium | mitigate | `buildCandidateId` hashes `signalType|ticker|asOfDate|score|generatedAt`; the `generatedAt` component supplies the entropy, asserted by the tracer test's id-format regex and a dedicated collision case |
| T-11-02-04 | Tampering | Untrusted Yahoo `^VIX` payload drives sizing | high | mitigate | Reject a non-finite or non-positive close and fall through to the cache, then to the conservative `"elevated"` fallback — never `"calm"`; record `vixSource` on every candidate so a fallback-sized position is auditable |
| T-11-02-05 | Denial of Service | Yahoo fetch hang stalls `strategy run` | low | mitigate | The existing provider already sets a 30s axios timeout (`yahoo-finance-provider.ts:78`); the cache short-circuits repeat calls for the same date |
| T-11-02-06 | Tampering | Substrate read races `intel backlog-drain` rewriting rollups | medium | mitigate | `isSubstrateHot()` checks `isDrainLocked(intelDataDir)` before reading a day inside 48 hours and the CLI prints a warning that the day's rollups may be mid-rebuild (RESEARCH Pitfall 4) |
| T-11-02-07 | Repudiation | Decision history altered after the fact | medium | mitigate | Append-only writes with reconcile-at-read; no code path rewrites or deletes an existing `decisions-*.jsonl` row |
| T-11-02-08 | Information Disclosure | `data/strategy/` contents | low | accept | Contains derived market data and the operator's own trade decisions on a single-operator local machine; no credential is written and no new secret is introduced |
| T-11-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages (RESEARCH § Package Legitimacy Audit). If an install becomes necessary, stop and run the Package Legitimacy Gate before adding it |
</threat_model>

<verification>
- `pnpm vitest run` — full suite green
- `pnpm tsc --noEmit` and `pnpm biome check src/` — clean
- `pnpm dev strategy run --date 2026-08-26 --dry-run` — runs against real `data/intel`, prints VIX header, ranked section, sub-threshold section
- `pnpm dev strategy --help` — lists exactly the seven subcommands this plan ships
- No file under `src/strategy/` carries an `import` line referencing `market-intelligence/scheduler/` — the engine is not wired into `runCycle` (D-21). Import-scoped: `node -e "const{execSync}=require('child_process');const fs=require('fs');const f=execSync('find src/strategy -name \"*.ts\"').toString().trim().split('\n');process.exit(f.some(p=>/^\s*import[^;]*market-intelligence\/scheduler/m.test(fs.readFileSync(p,'utf8')))?1:0)"` exits 0
- The engine exposes no exit-signal API surface (D-10): `src/strategy/types.ts` declares no exit/close-signal type and `StrategyCandidate` has no exit-instruction field — the only close path is the operator-invoked `DecisionLog.recordClose`

There is no SPEC for this phase, so there is no SPEC Edge Coverage or Prohibitions section to lift; the criteria above are derived from the phase goal and the CONTEXT decisions in the decision map.
</verification>

<success_criteria>
1. A SECTOR_ROTATION_FROM_PM candidate flows from a real `ticker-day-summary` row to a ranked, sized, level-bearing CLI line and an accepted `decisions-*.jsonl` record
2. Score floor 0.4, top-5 cap and next-3 sub-threshold reporting behave exactly as D-05 states, including the honest empty case
3. VIX regime sizing produces 1875 / 937 / 468 dollars on the 7500 assumed equity, with the FADE 50% modifier present and tested
4. Entry/target/stop cover all four types' formulas with Pitfall-5 clamps on ATR-derived levels only
5. The decision log is append-only and reconciles by `candidateId` at read time, including close records with a realized P&L
6. `SignalTypeModule` is a stable contract that 11-03 and 11-04 can implement without editing engine internals
</success_criteria>

<output>
Create `.planning/phases/11-ai-strategy/11-02-SUMMARY.md` when done. Record: the `SignalTypeModule` and `RawSignal` signatures as shipped (downstream plans depend on them verbatim), the `candidates-*.jsonl` and `decisions-*.jsonl` record shapes, the observed output of the real-data `strategy run` smoke, and any ATR-multiplier tuning applied under Claude's discretion.
</output>
