---
phase: 11
plan: 08
type: execute
wave: 5
depends_on: ["11-06", "11-07"]
files_modified:
  - src/strategy/__tests__/worked-example.test.ts
  - .planning/phases/11-ai-strategy/11-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [INCOME-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-22: the canonical happy path walks end to end in one automated test: an Iran-ceasefire-shaped PM signal becomes a SECTOR_ROTATION_FROM_PM XLE candidate with suggested levels, the operator accepts with overrides, the decision is logged, and a manual close records a realized P&L"
    - "D-15: the full-window live-window backtest is run and its combined and per-core-type numbers are recorded against the thresholds — combined Sharpe > 0, MaxDD < 25% — labelled interim, not the per-regime bar"
    - "D-16: the article-intake pre-screen's >= 85% held-out retention is re-confirmed as part of phase acceptance"
    - "Every v1 mode is observable in one real run: two core types ranked and sized, the gated type either ranked or refusing with a stated reason, the shadow type logged without a size"
    - "The phase closes with STATE.md and ROADMAP.md reflecting what actually shipped, including the honestly-labelled backtest result whether it passed or failed"
  artifacts:
    - src/strategy/__tests__/worked-example.test.ts
    - "updated .planning/phases/11-ai-strategy/11-VALIDATION.md per-task status column"
    - "updated .planning/STATE.md and .planning/ROADMAP.md"
  key_links:
    - "the worked example uses the real 36-ticker substrate universe and the Iran/XLE fixture shape from M2-04's acceptance test, not an invented universe"
    - "the acceptance run exercises the same CLI the operator uses daily, not a test-only code path"
---

<objective>
Close the phase: prove the canonical end-to-end happy path in one automated test, run the acceptance gate for real, and record what shipped — including the backtest verdict, whatever it turns out to be.

Purpose: every prior plan verified its own layer. This one verifies the promise: that a real-money-market sentiment signal becomes a specific trade idea with levels and a size, that the operator's decision on it is durable, and that the whole thing measures out to a number that is honestly labelled.

Output: `worked-example.test.ts`, an updated validation map, and updated project state.
</objective>

<decision_map>
| ID | CONTEXT.md source |
|----|-------------------|
| D-15 | `### Backtest acceptance (2026-08-27)` — the live-window gate is the v1 acceptance check |
| D-16 | `### Article intake thrift (2026-08-27)` — the >= 85% retention bar |
| D-02 | `### v1 signal set (2026-08-27)` — core / gated / shadow modes, all three observable in one run |
| D-22 | `<specifics>` — "The Iran worked example from M2-04's acceptance fixture should land naturally in M2-05 ... M2-05's worked example test should walk it" |
</decision_map>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-strategy/11-CONTEXT.md
@.planning/phases/11-ai-strategy/11-05-SUMMARY.md
@.planning/phases/11-ai-strategy/11-06-SUMMARY.md
@.planning/phases/11-ai-strategy/11-07-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/strategy/__tests__/worked-example.test.ts` — the canonical Iran-ceasefire -> XLE -> accept -> close walkthrough
- Updated `.planning/phases/11-ai-strategy/11-VALIDATION.md` (per-task Status column, Wave 0 checkboxes, sign-off checkboxes)
- Updated `.planning/STATE.md` (M2-05 status, session-history row, next actions) and `.planning/ROADMAP.md` (M2-05 status line, plan checkboxes)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The canonical worked example — Iran ceasefire to a closed XLE trade</name>
  <files>src/strategy/__tests__/worked-example.test.ts</files>
  <read_first>
    - `src/strategy/strategy-engine.ts`, `src/strategy/decision-log.ts`, `src/strategy/signals/sector-rotation.ts`, `src/strategy/types.ts` (11-02/11-05 output)
    - `src/market-intelligence/signal/__tests__/pm-mapping-engine.test.ts` (M2-04's acceptance fixture — the Iran-ceasefire market shape, slugs and pp values to reuse in shape though not verbatim, since the dates have moved on)
    - `config/pm-market-mappings.json` (the live Iran-family rules: `will-the-us-invade-iran`, `us-announces-blockade-on-iran`, `iran-full-airspace-closure`, `iran-charges-hormuz-fees`, and their `tickers[]` with XLE/USO/JETS directions and weights)
    - `.planning/phases/11-ai-strategy/11-RESEARCH.md` §0 (the real 36-ticker substrate universe: `AAPL, AMZN, COIN, GLD, GOOGL, IBIT, IEF, IWM, JETS, JPM, LMT, META, MSFT, MSTR, NFLX, NVDA, QQQ, RTX, SLV, SPY, TLT, TSLA, UNG, USO, V, XLB, XLC, XLE, XLF, XLI, XLK, XLP, XLRE, XLU, XLV, XLY`)
    - `src/market-intelligence/signal/__tests__/rollup-backfill.test.ts` (the `fs.mkdtemp` + fixture-file test shape)
    - `.planning/phases/11-ai-strategy/11-CONTEXT.md` `<specifics>` (the worked-example narrative this test must walk)
  </read_first>
  <behavior>
    - A `ticker-day-summary` fixture carrying an Iran-family PM contribution for XLE produces exactly one ranked `SECTOR_ROTATION_FROM_PM` XLE long candidate.
    - That candidate's score equals the hand-computed D-03 formula value for the fixture's `movePp` / `weight` / `volume24hr`, asserted to a literal number rather than a range.
    - Its rationale names the source market slug and the pp move, so the operator can see why XLE.
    - Its `suggestedSizeUsd` matches the VIX regime the stub provider returns, and `stop < entry < target` for the long direction.
    - Accepting with an operator entry override persists the operator's number and leaves the engine suggestion intact in its own field.
    - Closing at a profitable exit price writes a record with the same `candidateId`, a positive `closeRealizedPnlUsd`, and a `closeRealizedPnlPct` consistent with the operator's entry.
    - Reading the log back with `readDedupedByCandidateId` returns exactly one reconciled record for that `candidateId`, carrying the close fields.
    - Tickers outside the fixture's PM contribution produce no candidate — the fixture's other 35 universe members stay silent.
  </behavior>
  <action>
    Write `src/strategy/__tests__/worked-example.test.ts` as a single narrative test walking CONTEXT's canonical happy path in order: PM signal -> candidate -> suggested levels -> operator accepts -> decision logged -> operator closes -> outcome recorded.

    Build the fixture from the real universe and the real mapping rules: an `fs.mkdtemp` data directory with a `ticker-day-summary-2026-06-25.jsonl` containing an XLE row whose `pmContribution.sources` carries an Iran-family market (slug from the live `config/pm-market-mappings.json` family, `movePp` around 12, `direction: "long"`, `weight` matching the config rule, `volume24hr` around 2,000,000) plus several other universe tickers with no PM contribution. Reuse the M2-04 acceptance fixture's *shape*, not its exact record — the dates have moved on since that fixture was written.

    Inject deterministic stubs for VIX and market data so the assertion values are literals, not ranges. Assert each `<behavior>` bullet in sequence, with a comment above each block naming the step of the narrative it covers, so a future reader can follow the story through the assertions.

    Do not assert on any exit *signal* — v1 emits none (D-10). The close in this test is the operator's manual action, which is the whole point of the "suggest levels, operator executes and manages" model.
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy/__tests__/worked-example.test.ts && pnpm vitest run && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy/__tests__/worked-example.test.ts` exists and covers every `<behavior>` bullet in narrative order
    - The score assertion is a literal number derived by hand from the D-03 formula, with the arithmetic shown in a comment
    - A test asserts the reconciled record carries both the operator's entry and a positive realized P&L
    - `pnpm vitest run` exits 0 (full suite); `pnpm tsc --noEmit` and `pnpm biome check src/` exit 0
  </acceptance_criteria>
  <done>The canonical Iran-ceasefire happy path is an automated, deterministic regression test from PM signal to closed trade.</done>
  <reversibility rating="reversible">Test-only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the acceptance gate for real and complete the validation map</name>
  <files>.planning/phases/11-ai-strategy/11-VALIDATION.md</files>
  <precondition>All of 11-01 through 11-07 are merged, `pnpm vitest run` is green, and `data/intel` covers 2026-05-23 → the current date with `intel backlog-drain` not running.</precondition>
  <read_first>
    - `.planning/phases/11-ai-strategy/11-VALIDATION.md` (the per-task verification map, the Wave 0 checklist, the sign-off checklist)
    - `.planning/phases/11-ai-strategy/11-06-SUMMARY.md` (11-06's recorded backtest numbers — this task re-runs over the full default window and compares)
    - `.planning/phases/11-ai-strategy/11-01-SUMMARY.md` (the recorded pre-screen retention figures)
    - `docs/M2-05_BACKTEST_GAP.md` (11-06 output)
  </read_first>
  <action>
    Execute the phase-gate checks from `11-VALIDATION.md` § Sampling Rate and record every result.

    1. `pnpm vitest run` — full suite; record the pass count against the 380 baseline in STATE.md.
    2. `pnpm tsc --noEmit` and `pnpm biome check src/` — record clean/dirty.
    3. `pnpm dev strategy backtest` over the full default window — record per-type and combined Sharpe, MaxDD, trade count, thin-sample markers, and the PASS/FAIL verdict against D-15's thresholds (combined Sharpe > 0, MaxDD < 25%). If it fails, record the failure and its numbers; do not tune to pass. A failing honest measurement is a legitimate phase outcome and is what the 11-08 checkpoint exists to adjudicate.
    4. `pnpm intel prescreen-eval --start 2026-07-22 --end 2026-07-26` — re-confirm D-16's >= 85% retention bar still holds after all merges.
    5. `pnpm dev strategy run --date <today>` — record the live run: ranked count and their types/scores, sub-threshold scores, shadow count, and any `skippedTypes` reasons. Confirm all three v1 modes are observable in this single output: two core types ranked and sized, SENTIMENT_VELOCITY either ranked or refusing with its coverage reason, FADE_OVERSHOOT present in the shadow section with no size.
    6. `pnpm dev strategy decisions-summary --days 30` — confirm it runs and reports the accept-rate band (the rate itself will be near-empty this early; the point is the report path works).

    Then update `.planning/phases/11-ai-strategy/11-VALIDATION.md`: set each per-task-map row's `File Exists` and `Status` columns to reflect reality (✅ green / ❌ red / ⚠️ flaky), tick the Wave 0 Requirements boxes, tick the Validation Sign-Off boxes that now hold, and set `nyquist_compliant: true` in the frontmatter only if every row is green with an automated command. Leave the `status:` frontmatter field alone — the validate-phase workflow owns it.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm tsc --noEmit && pnpm biome check src/ && pnpm intel prescreen-eval --start 2026-07-22 --end 2026-07-26</automated>
  </verify>
  <acceptance_criteria>
    - `11-VALIDATION.md`'s per-task map has no row left with a `11-XX-XX` placeholder Task ID or a `TBD` Plan
    - Every row's Status column is ✅, ❌ or ⚠️ — none left ⬜ pending
    - The SUMMARY records: full-suite pass count, backtest per-type and combined numbers with the verdict, the re-confirmed pre-screen retention, and the live `strategy run` output
    - All three v1 signal modes are demonstrably present in the recorded live run output
    - `pnpm intel prescreen-eval --start 2026-07-22 --end 2026-07-26` prints `PASS`
  </acceptance_criteria>
  <done>The phase's own acceptance criteria have been executed against real data and the results are written down, pass or fail.</done>
  <reversibility rating="reversible">Measurement and documentation.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator accepts the phase and its honest numbers</name>
  <what-built>
    A four-signal-type strategy engine on top of the M2-04 substrate: CATALYST_ANCHORED and SECTOR_ROTATION_FROM_PM ranked and sized, SENTIMENT_VELOCITY gated on real scored-article coverage, FADE_OVERSHOOT logged shadow-only; VIX-regime dollar sizing with a 7500 assumed-equity fallback; per-type entry/target/stop; an append-only decision log with accept/skip/close and a 30-day accept-rate report; a nine-subcommand `strategy` CLI; a minimal `/strategy` web route; a live-window backtest gate labelled as interim rather than the per-regime bar; a written record of why the per-regime bar is unevaluable; and a pre-LLM article-intake pre-screen that retains >= 85% of high-materiality articles at half the scoring budget.
  </what-built>
  <how-to-verify>
    1. Read Task 2's recorded numbers in `11-08-SUMMARY.md` — especially the combined and per-core-type Sharpe / MaxDD / trade count and the PASS/FAIL verdict.
    2. Read `docs/M2-05_BACKTEST_GAP.md` and confirm you agree with how the deferred per-regime work is characterised.
    3. Run today's engine yourself: `pnpm dev strategy run` — do the candidates look like trades you would actually consider? Is the rationale enough to judge each one?
    4. Confirm the empty/quiet-day behavior is what you want: pick a quiet date and check that it says nothing cleared the threshold rather than offering you filler.
    5. Confirm the shadow section reads as "we are watching this, we are not sizing it".
    6. Decide the two open calibration questions CONTEXT left to first-week data: is the 0.4 score floor right, and are the VIX regime boundaries (15 / 25) right for how 2026 has actually traded?
    7. Confirm the pre-screen change to live scoring is acceptable: `pnpm intel prescreen-eval --start 2026-07-22 --end 2026-07-26`.
  </how-to-verify>
  <resume-signal>Type "approved", or state which numbers or behaviors need to change before this phase closes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded acceptance numbers → operator's trading decisions | A measurement written into project state will be trusted later as evidence |
| `.planning/` state files → future planning sessions | STATE.md and ROADMAP.md are read as ground truth by every subsequent phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Repudiation | A backtest number recorded without its caveats and later read as the per-regime bar | high | mitigate | Every recorded figure is written alongside `LIVE_WINDOW_LABEL`, its trade count, its thin-sample marker and a link to `docs/M2-05_BACKTEST_GAP.md`; the blocking checkpoint puts the numbers in front of the operator before the phase closes |
| T-11-08-02 | Tampering | Tuning parameters until the acceptance gate passes | high | mitigate | Task 2 states explicitly that a failing measurement is recorded as-is and adjudicated at the checkpoint; the gate is a measurement, not a target |
| T-11-08-03 | Tampering | A non-deterministic worked example that passes for the wrong reason | medium | mitigate | VIX and market-data stubs are injected, the score is asserted as a hand-computed literal with its arithmetic shown, and the fixture uses the real universe and real mapping-rule slugs |
| T-11-08-04 | Information Disclosure | `.planning/` and SUMMARY files | low | accept | Local project documentation; no credential is recorded |
| T-11-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages. If an install becomes necessary, stop and run the Package Legitimacy Gate first |
</threat_model>

<verification>
- `pnpm vitest run`, `pnpm tsc --noEmit`, `pnpm biome check src/` — all clean
- `pnpm dev strategy backtest` completes over the full window and its numbers are recorded with the interim label
- `pnpm intel prescreen-eval --start 2026-07-22 --end 2026-07-26` prints PASS
- `11-VALIDATION.md` has no placeholder Task IDs and no pending Status cells
- The human-verify checkpoint is approved

There is no SPEC for this phase, so there is no SPEC Edge Coverage or Prohibitions section to lift; the criteria above are derived from the phase goal and the CONTEXT decisions in the decision map.
</verification>

<success_criteria>
1. The Iran-ceasefire worked example passes end to end as a deterministic automated test
2. The live-window backtest is run over the full window and its per-type and combined numbers are recorded against D-15's thresholds, labelled interim
3. The pre-screen's >= 85% retention bar is re-confirmed after all merges
4. All three v1 signal modes are observable in one real `strategy run` output
5. `11-VALIDATION.md`, `STATE.md` and `ROADMAP.md` reflect what actually shipped
6. The operator has seen the numbers and approved, or has named what must change
</success_criteria>

<output>
Create `.planning/phases/11-ai-strategy/11-08-SUMMARY.md` when done. Record: the full acceptance transcript (suite counts, backtest table, verdict, prescreen retention, live run output), the operator's answers to the two calibration questions (0.4 floor, VIX boundaries), and the checkpoint verdict. Then update `.planning/STATE.md` and `.planning/ROADMAP.md`.
</output>
