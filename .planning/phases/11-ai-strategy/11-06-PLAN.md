---
phase: 11
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - src/strategy/backtest/live-window-runner.ts
  - src/strategy/backtest/__tests__/live-window-runner.test.ts
  - src/strategy/cli/strategy-commands.ts
  - src/strategy/cli/__tests__/strategy-commands.test.ts
  - docs/M2-05_BACKTEST_GAP.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [INCOME-01]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-15: the v1 acceptance gate runs the engine day-by-day over the real 2026-05-23 → run-date substrate and reports Sharpe / MaxDD / trade count through PerformanceMetricsCalculator.calculate() with no regime slicing"
    - "D-15: every backtest output is labelled 'single continuous 2026 window — interim, not the per-regime bar', so no reader mistakes it for the CONTEXT-locked 2018-2025 bar"
    - "D-15 thresholds: combined Sharpe > 0 and MaxDD < 25% over the window, with CATALYST_ANCHORED and SECTOR_ROTATION_FROM_PM each also reported individually"
    - "D-15 (E3): the structural reason the per-regime bar is unevaluable, and the two paths that would unlock it, are written down as a durable deliverable rather than left in a research doc"
    - "Trade count is printed beside every Sharpe, and any Sharpe from fewer than 20 closed trades is labelled directional-only"
    - "FADE_OVERSHOOT is counted but never enters the equity curve, because a shadow candidate has no size"
  artifacts:
    - src/strategy/backtest/live-window-runner.ts
    - src/strategy/backtest/__tests__/live-window-runner.test.ts
    - docs/M2-05_BACKTEST_GAP.md
    - "strategy backtest CLI subcommand"
  key_links:
    - "live-window-runner calls PerformanceMetricsCalculator.calculate() directly and never touches regime-segmenter's REGIMES, because no window in it covers calendar 2026"
    - "the runner reuses loadPmSignalsForDay and rebuildRollupForDay from rollup-backfill.ts rather than re-deriving PM signals"
    - "per-type runs go through strategy run --types, the same code path the operator uses live"
    - "isDrainLocked is checked before the run so a concurrent intel backlog-drain cannot corrupt the measured window"
---

<objective>
Build the honest acceptance gate CONTEXT settled on for v1: replay the engine day by day across the real 2026 substrate, price the resulting candidates against real OHLCV, and report Sharpe / max drawdown / trade count per signal type and combined — labelled, unambiguously, as a single continuous 2026 window rather than the per-regime bar the ROADMAP originally asked for. Then write down why the original bar cannot be evaluated and what would unlock it.

Purpose: `regime-segmenter.ts`'s `REGIMES` constant covers 2018-2025 only; every M2-04 stream starts 2026-05-23. No date range satisfies both "same regimes as M2-01" and "uses historical M2-04 outputs", for any signal type. The operator confirmed this on 2026-08-27 and replaced the bar. What remains is to build the replacement honestly and leave a paper trail so the deferred work is recoverable.

Output: `live-window-runner.ts`, the `strategy backtest` subcommand, and `docs/M2-05_BACKTEST_GAP.md`.

Runs in Wave 4 in parallel with 11-07; the two plans share no files.
</objective>

<decision_map>
| ID | CONTEXT.md source |
|----|-------------------|
| D-15 | `### Backtest acceptance (operator decision, 2026-08-27)` — live-window gate over the real 2026 substrate via `PerformanceMetricsCalculator.calculate()`, labelled interim; thresholds combined Sharpe > 0 and MaxDD < 25%; core types reported individually; the true per-regime backtest deferred |
| D-10 | `### Position Management` — no exit signals in v1, so the backtest's exits are mechanical target/stop/horizon rules, not engine-emitted instructions |
| D-02 | `### v1 signal set` — the shadow type is counted, never sized, so it cannot appear in the equity curve |
</decision_map>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-strategy/11-CONTEXT.md
@.planning/phases/11-ai-strategy/11-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**New source symbols**
- `src/strategy/backtest/live-window-runner.ts`
  - `export interface LiveWindowOptions { startIso; endIso; types?: SignalType[]; initialCapital?; slippagePctPerSide?; commissionPerTrade?; intelDataDir?; strategyDataDir? }`
  - `export interface LiveWindowReport { window; label; perType: Record<SignalType, TypeReport>; combined: TypeReport; shadowCandidateCount; skippedDays }`
  - `export interface TypeReport { signalType | "COMBINED"; candidateCount; tradeCount; metrics: PerformanceMetrics; thinSample: boolean; usableRange: string }`
  - `export async function runLiveWindow(options): Promise<LiveWindowReport>`
  - `export function simulateCandidate(candidate, bars, costs): Trade | null`
  - `export const LIVE_WINDOW_LABEL` — the exact interim-labelling string
  - `export const THIN_SAMPLE_TRADE_THRESHOLD = 20`
- **New CLI subcommand**: `strategy backtest [--start YYYY-MM-DD] [--end YYYY-MM-DD] [--types <csv>] [--out <path>]`
- **New doc**: `docs/M2-05_BACKTEST_GAP.md`
- **New output file**: `data/strategy/backtest-<start>_<end>.json` when `--out` is omitted
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Live-window runner — replay one signal type across real days into real metrics</name>
  <files>src/strategy/backtest/live-window-runner.ts, src/strategy/backtest/__tests__/live-window-runner.test.ts</files>
  <precondition>`data/intel/ticker-day-summary-*.jsonl` and `data/intel/polymarket-snapshots-*.jsonl` cover 2026-05-23 → the current date, and `intel backlog-drain` is not running (`data/intel/score-backlog.lock` absent or stale). Verify with `ls data/intel/ticker-day-summary-2026-05-31.jsonl` and `ls data/intel/score-backlog.lock` before starting.</precondition>
  <read_first>
    - `src/backtesting/analytics/performance-metrics.ts` lines 1-60 (`PerformanceMetricsCalculator.calculate(equityCurve, trades, initialCapital, startDate, endDate, totalCommissions, totalSlippage)` and the fact that Sharpe is computed from `equityCurve[].returns`)
    - `src/backtesting/types/backtest-types.ts` lines 218-244 (`EquityCurvePoint`: `timestamp`, `equity`, `cash`, `positionsValue`, `cumulativeReturn`, `returns?`, `dailyReturn`, `drawdown`) and lines 246-300 (`Trade`: `id`, `symbol`, `type`, `entryPrice`, `quantity`, `exitPrice`, `exitReason`, `commission`, `slippage`, `pnl`, `pnlPercent`, `holdingPeriod`)
    - `src/backtesting/analytics/regime-segmenter.ts` lines 86-101 (the `REGIMES` constant whose windows all end 2025-12-31 — read it so the reason this runner bypasses it is concrete, then do not import it)
    - `src/market-intelligence/signal/rollup-backfill.ts` lines 53-140 (`backfillMissingRollups` day-iteration shape, `rebuildRollupForDay`, `loadPmSignalsForDay`)
    - `src/market-intelligence/signal/backlog-drain.ts` lines 34-131 (`isDrainLocked`, `DRAIN_LOCK_FILE`, staleness window)
    - `src/backtesting/strategies/strategy-adapter.ts` lines 1-50 (the newest-first vs oldest-first bar-ordering trap that silently broke M2-01's SELL conditions)
    - `src/data/market-data-service.ts` lines 473-530 (`fetchHistoricalData`, provider fallback, Yahoo returning newest-first)
    - `src/strategy/strategy-engine.ts` and `src/strategy/types.ts` (11-05 output)
    - `.planning/phases/11-ai-strategy/11-RESEARCH.md` §8 (the whole rewritten backtest section incl. the per-type usable-range table) and §9 Pitfall 3
  </read_first>
  <behavior>
    - `simulateCandidate` on a long candidate whose bars hit the target before the stop returns a `Trade` with `exitReason: "TAKE_PROFIT"` and a positive `pnl`.
    - When a single bar's range spans both the stop and the target, the stop wins — the pessimistic assumption, since intraday sequence is unknowable from daily bars.
    - A candidate whose bars never touch either level exits at the close of the final bar within `timeHorizonDays` with `exitReason: "TIME_LIMIT"`.
    - A short candidate profits when the exit price is below the entry.
    - Slippage is applied on both sides at `slippagePctPerSide` (default 0.0005) and commission is 0 by default, matching the M2-01 cost model; both are reported in the `Trade` and summed into the metrics call.
    - `quantity` is `Math.floor(suggestedSizeUsd / entryPrice)`, and a candidate whose size buys fewer than 1 share returns `null` rather than a zero-quantity trade.
    - A candidate with `suggestedSizeUsd === null` (sub-threshold or shadow) returns `null` and never reaches the equity curve.
    - `runLiveWindow` produces an `EquityCurvePoint` for every calendar day in the window with `returns` set to that day's decimal return, so `PerformanceMetricsCalculator` can compute Sharpe.
    - At most `config.maxSimultaneousPositions` (4) positions are open at once; a candidate arriving when four are open is counted but not traded, and the count is reported.
    - A day whose substrate is missing is recorded in `skippedDays` rather than silently contributing a flat return.
    - `runLiveWindow` never imports `regime-segmenter`.
  </behavior>
  <action>
    Create `src/strategy/backtest/live-window-runner.ts` implementing D-15's replacement gate.

    Day loop, following the `rollup-backfill.ts` iteration shape: for each calendar day from `startIso` to `endIso`, construct a `StrategyEngine` scoped to that day and call `generateCandidates(day)`. Filter to `options.types` when supplied by passing the same type filter the `strategy run --types` path uses, so the backtest exercises the operator's real code path rather than a parallel one. Where a day's `ticker-day-summary` row is missing or stale, re-derive PM signals with `loadPmSignalsForDay(intelDataDir, day, engine)` and rebuild with `rebuildRollupForDay` — reuse those helpers, do not reimplement PM-signal reconstruction.

    Before the loop, call `isDrainLocked(intelDataDir)`; when locked, abort with a clear message naming `score-backlog.lock` rather than measuring a window that is being rewritten underneath the run (RESEARCH Pitfall 4).

    Price simulation: fetch each candidate ticker's OHLCV once for the whole window plus a 20-bar tail via `MarketDataService.fetchHistoricalData`, **sort ascending by date with an explicit rationale comment naming the newest-first Yahoo ordering** (this is the exact trap that silently broke M2-01's SELL conditions — `strategy-adapter.ts` documents it), and index by `YYYY-MM-DD`. `simulateCandidate(candidate, bars, costs)` enters at `suggestedEntry` on the first bar after `asOfDate`, walks forward at most `timeHorizonDays` bars, and applies the exit rules in `<behavior>`.

    Portfolio accounting: start at `initialCapital` (default `config.assumedEquity`), cap concurrency at `config.maxSimultaneousPositions`, mark equity daily, and build the `EquityCurvePoint[]` with `returns` populated. Then call `PerformanceMetricsCalculator.calculate(equityCurve, trades, initialCapital, startDate, endDate, totalCommissions, totalSlippage)` **directly** — no `sliceByRegime`, no `metricsByRegime`. Add a doc comment on the call stating why: `REGIMES` has no calendar-2026 window, so slicing would return empty sets for every regime.

    Shadow candidates are counted into `shadowCandidateCount` and excluded from trading — they have no size, so they cannot be traded (D-02).

    `LIVE_WINDOW_LABEL` is the exact string `"single continuous 2026 window — interim, not the per-regime bar"`, exported as a constant so the runner, the CLI and any consumer print the identical wording.

    `thinSample` is `tradeCount < THIN_SAMPLE_TRADE_THRESHOLD` (20), per RESEARCH Pitfall 3 — Sharpe's standard error scales with 1/sqrt(N) and this window has at most ~90 daily observations.

    Write `src/strategy/backtest/__tests__/live-window-runner.test.ts` covering every `<behavior>` bullet against synthetic bars and stub modules with deterministic candidates, plus one `describe.skipIf(!existsSync("./data/intel"))` block running a 10-day real-substrate window end to end and asserting the report is well-formed (finite Sharpe or an explicit zero-trade report, `label === LIVE_WINDOW_LABEL`, `skippedDays` is an array).
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy/backtest/__tests__/live-window-runner.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/strategy/backtest/live-window-runner.ts` contains `export async function runLiveWindow(`, `export function simulateCandidate(`, `export const LIVE_WINDOW_LABEL`, `export const THIN_SAMPLE_TRADE_THRESHOLD`
    - The runner has no regime-windowing import: `node -e "const s=require('fs').readFileSync('src/strategy/backtest/live-window-runner.ts','utf8');process.exit(/^\s*import[^;]*regime-segmenter/m.test(s)?1:0)"` exits 0 (the check is import-scoped on purpose — the file's doc comments are expected to *name* the module while explaining why it is bypassed)
    - A test asserts the stop wins when one bar spans both stop and target
    - A test asserts a `suggestedSizeUsd === null` candidate yields `null` from `simulateCandidate`
    - A test asserts concurrency never exceeds 4 open positions
    - The live 10-day block completes and reports `label === LIVE_WINDOW_LABEL`
    - `pnpm tsc --noEmit` and `pnpm biome check src/` exit 0
  </acceptance_criteria>
  <done>The engine's output over a real date range becomes a real equity curve and a real `PerformanceMetrics` object, with no regime machinery involved.</done>
  <reversibility rating="reversible">An offline analysis path; it writes only a report file and reads existing streams.</reversibility>
</task>

<task type="auto">
  <name>Task 2: `strategy backtest` CLI — per-type and combined numbers against the D-15 thresholds</name>
  <files>src/strategy/cli/strategy-commands.ts, src/strategy/cli/__tests__/strategy-commands.test.ts</files>
  <precondition>Task 1's runner passes its live 10-day block, and `data/intel` covers 2026-05-23 → the current date.</precondition>
  <read_first>
    - `src/strategy/backtest/live-window-runner.ts` (Task 1 output)
    - `src/strategy/cli/strategy-commands.ts` (11-02/11-05 output — the eight registered subcommands, the `--types` validation added in 11-05, the chalk/ora conventions)
    - `src/market-intelligence/cli/intel-commands.ts` lines 739-780 (the `backlog-drain` progress-reporting CLI wrapping a long batch operation — the analog for a multi-minute run)
    - `.planning/phases/11-ai-strategy/11-CONTEXT.md` `### Backtest acceptance` (thresholds and labelling) and `.planning/phases/11-ai-strategy/11-RESEARCH.md` §8 (the per-signal-type usable-range table)
  </read_first>
  <action>
    Register `strategy backtest [--start YYYY-MM-DD] [--end YYYY-MM-DD] [--types <csv>] [--out <path>]` — the ninth and last subcommand.

    Defaults per RESEARCH §8's usable-range table, applied per type rather than as one blanket window: SECTOR_ROTATION_FROM_PM from `2026-05-23`; CATALYST_ANCHORED from `2026-05-31`; SENTIMENT_VELOCITY from `2026-06-02` (its coverage gate will decline the uncovered days by itself, which is the correct behavior — record those declines in `skippedDays`); FADE_OVERSHOOT counted only. `--end` defaults to today. An explicit `--start` overrides all of them.

    Output, in this order:
    1. A header block printing `LIVE_WINDOW_LABEL` verbatim, the window, and the thin-sample caveat sentence: state that Sharpe from fewer than `THIN_SAMPLE_TRADE_THRESHOLD` closed trades is directional only and not a pass/fail signal on its own.
    2. A per-type table: signal type, usable range, candidate count, trade count, Sharpe, Sortino, max drawdown %, win rate, and a `thin-sample` marker where applicable. CATALYST_ANCHORED and SECTOR_ROTATION_FROM_PM must each appear on their own row (D-15). FADE_OVERSHOOT appears with its candidate count and dashes for the traded metrics, annotated shadow-only.
    3. A combined row over all sized types.
    4. A verdict block evaluating the D-15 thresholds: combined Sharpe > 0 and combined MaxDD < 25%. Print `PASS` / `FAIL` per threshold and an overall verdict, then repeat that this is the interim gate and not the per-regime bar.
    5. `skippedDays` count with the top few reasons.

    Write the full `LiveWindowReport` as JSON to `--out` or, by default, `data/strategy/backtest-<start>_<end>.json`, so 11-08's acceptance step and any later comparison have a machine-readable artifact.

    Use `ora` for progress across the day loop (the window is ~90 days x up to 4 types) and print the elapsed time, matching `backlog-drain`'s long-operation conventions.

    Then RUN it: `pnpm dev strategy backtest`. Record the actual per-type and combined numbers, the trade counts, and the verdict in the plan SUMMARY. If the combined Sharpe is <= 0 or MaxDD >= 25%, do NOT tune parameters to make it pass — record the failure with its numbers and flag it for the 11-08 acceptance checkpoint. A failing honest number is the deliverable; a passing tuned one is not.

    Finally, complete the `backtest` case in `src/strategy/cli/__tests__/strategy-commands.test.ts` that 11-05 left as a placeholder, so all nine subcommands are covered.
  </action>
  <verify>
    <automated>pnpm vitest run src/strategy && pnpm tsc --noEmit && pnpm dev strategy backtest --start 2026-08-01 --end 2026-08-20 --out /tmp/m2-05-backtest-smoke.json</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm dev strategy --help` lists all nine subcommands including `backtest`
    - `pnpm dev strategy backtest --start 2026-08-01 --end 2026-08-20 --out /tmp/m2-05-backtest-smoke.json` exits 0 and the output file parses as JSON containing `label`, `perType`, `combined`
    - The printed header contains the exact string `single continuous 2026 window — interim, not the per-regime bar`
    - The per-type table contains a row for `CATALYST_ANCHORED` and a row for `SECTOR_ROTATION_FROM_PM`
    - The verdict block prints a PASS/FAIL for combined Sharpe > 0 and for MaxDD < 25%
    - `pnpm dev strategy backtest --start not-a-date` exits with code 2
    - The `backtest` case in `strategy-commands.test.ts` passes and the placeholder assertion is removed
    - The SUMMARY records the full-window per-type and combined numbers and the verdict
  </acceptance_criteria>
  <done>The operator can run one command and see, per signal type and combined, what this engine would have done across the real 2026 substrate — correctly labelled as interim.</done>
  <reversibility rating="reversible">Reporting only; no persisted trading state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write the deferred-per-regime-backtest gap doc (RESEARCH E3)</name>
  <files>docs/M2-05_BACKTEST_GAP.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/11-ai-strategy/11-RESEARCH.md` §8 (the whole structural finding: the quoted `REGIMES` constant, why Option B does not rescue it, the two unlock paths) and §10 E3
    - `.planning/phases/11-ai-strategy/11-CONTEXT.md` `### Backtest acceptance (operator decision, 2026-08-27)`
    - `src/backtesting/analytics/regime-segmenter.ts` lines 86-101 (quote the constant accurately)
    - `config/pm-market-mappings.json` (the 12 rules keyed to 2026-specific event slugs — the reason a 2023-2025 replay needs net-new curation)
    - `docs/LM_STUDIO_OPS.md` (the existing project-docs tone and structure to match)
    - Task 2's recorded backtest numbers
  </read_first>
  <action>
    Write `docs/M2-05_BACKTEST_GAP.md` as a durable, standalone explanation — RESEARCH §10 marks E3 a deliverable, not an optional note, and a research document is not where a future reader will look.

    Sections:
    1. **What the ROADMAP asked for** — the M2-05 success criterion "Backtest covers same 2018-2025 universe + regimes as M2-01" and the CONTEXT-locked per-regime bar (Sharpe > 0.5, MaxDD < 25%, negative bull Sharpe = automatic fail, combined > best single type).
    2. **Why it cannot be evaluated** — quote `REGIMES` from `regime-segmenter.ts:86-101` and state the dates: every window ends 2025-12-31, every M2-04 stream starts 2026-05-23, so the intersection is empty for every signal type. Make clear this is structural, not a data-thinness problem that waiting fixes.
    3. **Why fetching Polymarket history does not rescue it** — the `/prices-history` endpoint exists and needs no auth, but all 12 rules in `config/pm-market-mappings.json` are keyed to 2026-specific event slugs (`will-the-us-invade-iran`, `fed-rate-hike-in-2026`, and the rest), so a 2023-2025 replay needs an entirely hand-curated mapping set — work comparable in size to the original PmMappingEngine seeding, not a fetch.
    4. **What shipped instead** — the live-window gate, its thresholds, its labelling, and the actual numbers from Task 2.
    5. **What would unlock the original bar** — path A: a 2023-2025 Polymarket re-mapping project as its own phase; path B: accumulate enough live 2026-onward history for calendar 2026 to itself be regime-classified and added to `REGIMES`. Give each an honest prerequisite and a rough size.
    6. **Decision record** — operator confirmed the substitution on 2026-08-27 (CONTEXT.md `### Backtest acceptance`); this document exists so that decision is recoverable without reading a phase-planning archive.

    Add the deferred per-regime backtest to `.planning/ROADMAP.md` as a future-work note under the M2-05 section, linking this document — do not create a new phase; that is the operator's call.
  </action>
  <verify>
    <automated>test -s docs/M2-05_BACKTEST_GAP.md && grep -q "2025-12-31" docs/M2-05_BACKTEST_GAP.md && grep -q "prices-history" docs/M2-05_BACKTEST_GAP.md && grep -q "single continuous 2026 window" docs/M2-05_BACKTEST_GAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/M2-05_BACKTEST_GAP.md` exists, is non-empty, and contains all six numbered sections
    - It quotes the `REGIMES` window end date `2025-12-31` and the substrate start date `2026-05-23`
    - It names both unlock paths and both prerequisites
    - It contains the actual per-type and combined numbers from Task 2's full-window run
    - `.planning/ROADMAP.md`'s M2-05 section links `docs/M2-05_BACKTEST_GAP.md`
  </acceptance_criteria>
  <done>The gap between what the ROADMAP asked for and what v1 could honestly measure is written down, with the numbers, the reasons, and the two ways out.</done>
  <reversibility rating="reversible">Documentation.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI args → long batch run + file write | `--start`, `--end`, `--types`, `--out` drive a multi-minute run and a JSON write |
| Yahoo / provider OHLCV → simulated P&L | Untrusted external price data becomes the measured performance of the strategy |
| Concurrent `intel backlog-drain` → measured window | A second process rewriting rollups mid-run silently changes the measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06-01 | Tampering | Backtest run against a window being rewritten by `intel backlog-drain` | high | mitigate | `isDrainLocked(intelDataDir)` is checked before the day loop and the run aborts naming `score-backlog.lock` rather than measuring a moving target (RESEARCH Pitfall 4) |
| T-11-06-02 | Tampering | Bar-order assumption silently inverting exit logic | high | mitigate | Bars are explicitly sorted ascending with a rationale comment naming the newest-first provider ordering, and a test asserts a target-then-stop sequence resolves correctly — this is the exact failure that broke M2-01's SELL conditions |
| T-11-06-03 | Repudiation | A reported Sharpe that cannot be reproduced or is mistaken for the per-regime bar | high | mitigate | `LIVE_WINDOW_LABEL` is a single exported constant printed verbatim in the CLI header and stored in the JSON report; the full report is persisted with its window, per-type ranges and trade counts |
| T-11-06-04 | Tampering | `--out <path>` write target | low | mitigate | Resolve with `path.resolve`; default under `data/strategy/`; the command is local-operator-only |
| T-11-06-05 | Tampering | Thin-sample Sharpe read as a pass/fail verdict | medium | mitigate | `thinSample` is computed at `< 20` closed trades, printed as a marker on every affected row, and stated in the header caveat |
| T-11-06-06 | Information Disclosure | Persisted backtest JSON | low | accept | Derived market data on a single-operator local machine; no credential |
| T-11-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages — Sharpe/Sortino/MaxDD come from the existing `PerformanceMetricsCalculator`, per RESEARCH § Don't Hand-Roll. If an install becomes necessary, stop and run the Package Legitimacy Gate first |
</threat_model>

<verification>
- `pnpm vitest run` — full suite green
- `pnpm tsc --noEmit` and `pnpm biome check src/` — clean
- `pnpm dev strategy backtest` completes over the full default window and writes a JSON report
- No file under `src/strategy/` carries a regime-windowing import: `node -e "const{execSync}=require('child_process');const f=execSync('find src/strategy -name \"*.ts\"').toString().trim().split('\n');const fs=require('fs');process.exit(f.some(p=>/^\s*import[^;]*regime-segmenter/m.test(fs.readFileSync(p,'utf8')))?1:0)"` exits 0
- `docs/M2-05_BACKTEST_GAP.md` exists with the six sections and the real numbers

There is no SPEC for this phase, so there is no SPEC Edge Coverage or Prohibitions section to lift; the criteria above are derived from the phase goal and CONTEXT decision D-15.
</verification>

<success_criteria>
1. The engine replays across the real 2026 substrate and produces a real equity curve, priced against real OHLCV with M2-01's cost model
2. Sharpe / MaxDD / trade count are reported per signal type and combined, through `PerformanceMetricsCalculator.calculate()` with no regime slicing
3. Every output carries the exact interim label and the thin-sample caveat
4. The D-15 thresholds (combined Sharpe > 0, MaxDD < 25%) are evaluated and reported honestly, tuned or not
5. `docs/M2-05_BACKTEST_GAP.md` records the structural gap, the numbers and both unlock paths
</success_criteria>

<output>
Create `.planning/phases/11-ai-strategy/11-06-SUMMARY.md` when done. Record: the full-window per-type and combined Sharpe / MaxDD / trade counts, the PASS/FAIL verdict against D-15's thresholds, the shadow candidate count, the skipped-day count and its top reasons, and how long the full run took.
</output>
