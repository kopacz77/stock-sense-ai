---
phase: 07-strategy-reality-check
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtesting/strategies/strategy-adapter.ts
  - src/cli/backtest-commands.ts
autonomous: true

must_haves:
  truths:
    - "StrategyAdapter and createStrategyFactory are importable from a non-CLI module"
    - "Existing CLI commands (backtest run, backtest compare, backtest optimize) still work unchanged"
    - "M2-01 runner script (Plan 05) will be able to construct strategies without depending on src/cli/"
    - "strategy-registry.ts is explicitly bypassed (its momentum defaults are broken — out of scope to fix here)"
  artifacts:
    - path: "src/backtesting/strategies/strategy-adapter.ts"
      provides: "Reusable StrategyAdapter class + createStrategyFactory function"
      exports: ["StrategyAdapter", "createStrategyFactory"]
      min_lines: 80
    - path: "src/cli/backtest-commands.ts"
      provides: "CLI commands now import StrategyAdapter and createStrategyFactory from the new module"
  key_links:
    - from: "src/cli/backtest-commands.ts"
      to: "src/backtesting/strategies/strategy-adapter.ts"
      via: "import { StrategyAdapter, createStrategyFactory }"
      pattern: "import.*StrategyAdapter.*backtesting/strategies"
---

<objective>
Move `StrategyAdapter` (currently file-local at `src/cli/backtest-commands.ts:28-83`) and `createStrategyFactory` (lines 837-865) into a new reusable module at `src/backtesting/strategies/strategy-adapter.ts`. Update the CLI to import them from the new location. No behavior changes — pure refactor for reusability.

Purpose: The M2-01 reality-check runner (Plan 05) must instantiate `MomentumStrategy` and `MeanReversionStrategy` and adapt them to the `BacktestStrategy` interface. Importing from a CLI command file (`src/cli/backtest-commands.ts`) creates an awkward upward dependency from a script to the CLI layer. Extracting these to `src/backtesting/strategies/` makes them properly reusable.

Note: `strategy-registry.ts` has broken `defaultParams` keys for `MomentumStrategy` (research pitfall #2). DO NOT fix that here — it's out of scope. The new factory must bypass the registry entirely.

Output: New `src/backtesting/strategies/strategy-adapter.ts` exporting `StrategyAdapter` (class) and `createStrategyFactory` (function). CLI commands re-import from the new path. All existing CLI behavior preserved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-strategy-reality-check/07-CONTEXT.md
@.planning/phases/07-strategy-reality-check/07-RESEARCH.md

@src/cli/backtest-commands.ts
@src/strategies/momentum-strategy.ts
@src/strategies/mean-reversion-strategy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/backtesting/strategies/strategy-adapter.ts</name>
  <files>src/backtesting/strategies/strategy-adapter.ts</files>
  <action>
    Create the directory `src/backtesting/strategies/` if it doesn't exist, then create `strategy-adapter.ts`.

    Copy the **exact** existing code from `src/cli/backtest-commands.ts`:
    - `class StrategyAdapter` (lines 28-83) — adapter implementing `BacktestStrategy` that wraps `MomentumStrategy` or `MeanReversionStrategy` (which expose `.analyze()`).
    - `function createStrategyFactory(strategyName: string): (params: Record<string, unknown>) => BacktestStrategy` (lines 837-865) — factory function with correct constructor-key mapping.

    Both must be `export`ed. Fix imports in the new file:
    - `import { MomentumStrategy } from '../../strategies/momentum-strategy.js';`
    - `import { MeanReversionStrategy } from '../../strategies/mean-reversion-strategy.js';`
    - `import type { BacktestStrategy } from '../types/backtest-types.js';` (or whatever the existing import path in backtest-commands.ts uses, relative-pathed correctly from the new location).
    - Any other types referenced (e.g., `HistoricalDataPoint`, `Signal`, `BacktestContext`) — import from their real source modules.

    Add a top-of-file JSDoc:
    ```
    /**
     * Reusable adapter that wraps MomentumStrategy/MeanReversionStrategy
     * (which expose .analyze()) into the BacktestStrategy interface
     * (generateSignal, onBar, initialize, cleanup).
     *
     * NOTE: This intentionally bypasses src/strategies/strategy-registry.ts —
     * the registry's momentum defaultParams have broken keys (emaPeriod,
     * rsiPeriod, ... instead of shortMA, longMA, ...). Fixing the registry
     * is out of scope for M2-01.
     */
    ```

    DO NOT modify the logic. Pure file relocation with corrected import paths.
  </action>
  <verify>
    `pnpm build` — TypeScript compiles clean.

    Verify exports:
    ```
    pnpm tsx -e "import { StrategyAdapter, createStrategyFactory } from './src/backtesting/strategies/strategy-adapter.js'; const f = createStrategyFactory('momentum'); const s = f({}); console.log(s.name);"
    ```
    Should print the strategy name without throwing.
  </verify>
  <done>
    File `src/backtesting/strategies/strategy-adapter.ts` exists with `StrategyAdapter` class and `createStrategyFactory` function exported. Build clean. Smoke test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/cli/backtest-commands.ts to import from the new module</name>
  <files>src/cli/backtest-commands.ts</files>
  <action>
    In `src/cli/backtest-commands.ts`:

    1. **Delete** the file-local `class StrategyAdapter` (lines ~28-83) and `function createStrategyFactory` (lines ~837-865).
    2. **Add** an import near the top: `import { StrategyAdapter, createStrategyFactory } from '../backtesting/strategies/strategy-adapter.js';`
    3. Verify all 4 internal usages remain working (research found references at lines 158, 261, 342, 441 — confirm these still resolve to the imported symbols).
    4. Do NOT change `getDefaultParameterRanges()` (lines ~870-884) — that stays in the CLI file (it's CLI-specific UX).
    5. Do NOT touch any other code.
  </action>
  <verify>
    `pnpm build` — TypeScript compiles clean.

    `pnpm lint` clean.

    Functional regression test — run each affected CLI command on a tiny dataset:
    ```
    # Assumes Plan 01 done OR cache already has SPY data
    pnpm start backtest run SPY --strategy momentum 2>&1 | tail -20
    pnpm start backtest compare SPY 2>&1 | tail -20
    ```
    Both should run without errors and print performance metrics. If data isn't cached, the runs will fail at data-fetch — that's acceptable for verification *if* the error is "no data" not "TypeError" / "undefined function".
  </verify>
  <done>
    `src/cli/backtest-commands.ts` no longer defines `StrategyAdapter` or `createStrategyFactory` locally — both are imported from `../backtesting/strategies/strategy-adapter.js`. All 4 existing internal call sites still resolve. `backtest run` and `backtest compare` CLI commands work unchanged.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds.
- `pnpm lint` clean.
- `pnpm tsx -e "import { StrategyAdapter, createStrategyFactory } from './src/backtesting/strategies/strategy-adapter.js'; ..."` confirms exports work.
- `pnpm start backtest run SPY --strategy momentum` runs without runtime errors (errors only acceptable if they're data-fetch related, not symbol resolution).
- Grep confirms no remaining `class StrategyAdapter` or `function createStrategyFactory` in `src/cli/backtest-commands.ts`.
</verification>

<success_criteria>
1. New file `src/backtesting/strategies/strategy-adapter.ts` exports `StrategyAdapter` class and `createStrategyFactory` function.
2. `src/cli/backtest-commands.ts` imports both from the new location and no longer defines them inline.
3. Behavior preserved: all CLI commands using `StrategyAdapter` or `createStrategyFactory` still work.
4. Build + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-strategy-reality-check/07-02-SUMMARY.md` documenting:
- New file location + line count.
- Confirmed export signatures (paste the class signature and function signature).
- CLI regression test result.
- Any small adjustments needed (e.g., type-only imports, path corrections).
</output>
